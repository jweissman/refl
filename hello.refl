# hello.refl

fmemo = []
fib = (x) => x<2 ? 1 : fibm(x-1) + fibm(x-2);
fibm(x){
    if(fmemo[x]==nil){
        fmemo[x]=fib(x)
    };
    return(fmemo[x]);
}
# echo = (x) => print("fib", x, fib(x));

i = 0;
max = 45;
while (i<max) { i=i+1; print(fib(i)); }
print(fmemo)