# hello.refl

# fib = (n) => {
#    if (n < 2) {
#        return(1)
#    } else {
#        return(fib(n-1) + fib(n-2))
#    }
# }

fib = (n) => n < 2 ? 1 : (fib(n-1) + fib(n-2));
# print(fib(1))
# print(fib(2))
# print(fib(3))
# print(fib(4))
# print(fib(5))
# print(fib(6))

until = (f,i,n) { f(i); if (i<n) { until(f,i+1,n); } }
echo = (x) => print(x,fib(x));
until(echo, 0, 13) # loop until...