class String {
    initialize(contents) {
        self._set(contents);
        # println("NEW STRING")
    }

    # to_a() { List.new(self._getChars()) }

    # [](i) {}

    all() { self.to_a() }
    length() { self.all().length() }

    reverse() {
        i=0; max=self.all().length();
        res=''
        while(i<max) {
            res=res+self[max-i-1]
            i=i+1;
        }
        res
    }

    # [](i) { self.to_a()[i] }

    words() {
        self.to_a().split(" ")
    }

    #split(delim) {
    #    fragments = []
    #    i=0; max=self.length();
    #    fragment = ''
    #    fragmentStart = 0
    #    while(i<max) {
    #        res=res+self[max-i-1]
    #        i=i+1;
    #    }
    #}
}