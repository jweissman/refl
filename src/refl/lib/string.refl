class String {
    initialize(contents) {
        self._set(contents);
    }

    reverse() {
        i=0; max=self.length();
        res=''
        while(i<max) {
            res=res+self[max-i-1]
            i=i+1;
        }
        res
    }
}