class List {
    initialize() {
        # print("NEW LIST")
    }

    first() { self[0] }
    last() { self[self.length()-1] }

    push(e) { self[self.length()]=e }

    each(fn) { 
        i=0;
        max=self.length()
        while (i<max) {
            fn(self[i])
            i=i+1;
        }
    }
}