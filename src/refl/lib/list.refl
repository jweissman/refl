class MyrArray {
    each(fn) { 
        i=0;
        while (i<self.length()) {
            fn(self.get(i))
        }
    }
}


# fruit = List.new()
# fruit.push("banana")
# fruit.push("apple")
# fruit.push("strawberry")
# println(fruit.all())