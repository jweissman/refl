class List {
    initialize() {
        self.arr = []
    }

    all() { self.arr }

    count() { 
        num=len(self.all())
        println("count", num)
        num
    }

    push(it) {
        println("PUSH", it)
        at = self.count()
        self.arr[at] = it
    }
}

fruit = List.new()
fruit.push("banana")
fruit.push("apple")
fruit.push("strawberry")
println(fruit.all())