# hello.refl

class Greeter {
    initialize(prompt) {
        self.prompt = prompt
    }

    welcome(subject) {
        println(self.prompt + subject)
    }
}

greeter = Greeter.new("Hello, ");
greeter.welcome("world");